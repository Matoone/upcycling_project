<div class="container">
  <div class="row justify-content-center mb-5 mt-3">
    <h1>Nos articles</h1>
  </div>
  <div class="row mb-3">
    <% @items.each do |item| %>
    <div class="col text-center" id="grid-col-<%= item.id %>">
      <%= item.name %>
      <!--%= render 'card', item: item %-->
    </div>
    <% end %>
  </div>
</div>
