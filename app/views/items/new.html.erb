  <% puts "(" * 200 %>
  <% puts @shop %>
  <%= form_with(model: Item.new, url: items_path) do |f| %>
          <%= render 'item_details_form', f: f %>
          <%= f.hidden_field :shop_id, :value => @shop.id %>
          
          <%= f.submit "Créer"%>
        <% end %>
